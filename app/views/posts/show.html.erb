<h1><%= @post.title %></h1>

<% if @post.category %>
	<h3>Category: <%= link_to("#{@post.category.name}", category_path(@post.category.id)) %></h3>
<% end %>
<!-- Add code here to link to the post's category. -->

<p><%= @post.description %></p>
